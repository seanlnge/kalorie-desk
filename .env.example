# Used by the Vite /api/latest proxy (local only).
# On Vercel set SNAPSHOT_BASE_URL (server env, no VITE_ prefix) to the same value.
VITE_SNAPSHOT_BASE_URL=https://your-bucket.s3.us-east-2.amazonaws.com
